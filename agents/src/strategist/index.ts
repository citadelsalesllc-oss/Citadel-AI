import { createStubAgent } from '../stub-agent.js';

export const strategyAgent = createStubAgent(
  'strategy-agent',
  'Marketing strategy, campaign planning, offer strategy, competitive positioning, and lead-generation recommendations',
);
