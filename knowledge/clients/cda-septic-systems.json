{
  "_devDataNotice": "DEVELOPMENT/TEST SEED DATA. This file intentionally contains ONLY the one fact actually given about this client (its company name) — see the Phase 2 build spec's data-integrity rule: never invent business facts, including in seed/demo data. Every other field is left absent. Populate real facts for this client through the knowledge-management API (POST/PATCH /clients, /clients/:id/services, /clients/:id/service-areas, /clients/:id/brand-profile, /clients/:id/seo-profile, /clients/:id/faqs, /clients/:id/marketing-notes) as they become actually known, not by editing invented values into this file.",
  "slug": "cda-septic-systems",
  "companyName": "CDA Septic Systems"
}
